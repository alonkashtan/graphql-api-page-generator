<%# Renders a Union type section. Locals should be view model of the union type %>
<%- include('buildingBlocks/header.ejs', locals) %>
<p class="content"><b> Possible Types: </b>
    <span> 
    <%- types.map(type=> "<a href='#" + type.basicType + "'>" + type.type + "</a>").join(" | ") %>
    </span>
</p>